Generate basic package structure, common files, and configuration files for the Frontend based on the following requirements.

# Project Creation Sequence

Create frontend folder → Create package structure under frontend → Generate configuration files based on React + Vite → Generate main page → Set Node version 22 → Run React

# Frontend Package Structure
When creating package structure under frontend, it must be created with the following structure:
```
/frontend                  # Integrated frontend project (React + Vite + TypeScript)
  /public
    /favicon.ico          # Favicon
  /src
    /components
      /common             # Global common components
        /Layout.tsx       # Common layout component
        /Loading.tsx      # Loading component
    /pages                # Page components (React Router-based)
      /HomePage.tsx       # Main home page (includes BC-specific navigation)
    /services             # API and business logic
      /api
        /client.ts        # Axios client basic configuration
        /interceptors.ts  # API interceptors
        /types.ts         # API-related types
    /types                # TypeScript type definitions
      /common.types.ts    # Common type definitions
      /api.types.ts       # API-related types
    /utils                # Utility functions
      /constants.ts       # Constant definitions (API endpoints, etc.)
    /theme                # Material-UI theme settings
      /index.ts           # Integrated theme settings
    /App.tsx              # Root App component
    /main.tsx             # Vite entry point
    /index.css            # Global styles
    /vite-env.d.ts        # Vite type definitions
  package.json            # NPM package configuration
  vite.config.ts          # Vite build tool configuration
  tsconfig.json           # TypeScript configuration
  .gitignore              # Git ignore file
  ```

# Main Page Generation
When generating the main page, it must be created with the following layout format:

## 🏠 Main Page Layout

### Full Window Layout
- **Container**: Width and height must be completely filled when opened in browser (no vh, vw 100 handling)
- **Box**: `sx=\{{ display: 'flex', minHeight: '100vh' }}` for flex layout
- **No Padding**: Remove default margins with `sx=\{{ m: 0, p: 0 }}`

### Main Page Structure
```typescript
<Box sx=\{{ display: 'flex', minHeight: '100vh', width: '100vw' }}>
  <Sidebar />
  <Box sx=\{{ flex: 1, p: 3 }}>
    <Container maxWidth="xl">
      {/* Main content */}
    </Container>
  </Box>
</Box>
```

## 🧭 Navigation & Routing

### Route Card System
- **Card Layout**: `Grid container spacing={3}` with responsive columns
- **Route Cards**: `Card` with `onClick` navigation and hover effects
- **Card Content**: Icon + title + description + action button
- **Responsive**: `xs={12} sm={6} md={4} lg={3}` for various screen sizes

## 📱 Sidebar Navigation

### Sidebar Structure
- **Fixed Width**: `width: 240px` on desktop, collapsible on mobile
- **Position**: `position: 'fixed'` with `left: 0` and `top: 0`
- **Height**: `height: '100vh'` for full viewport height
- **Z-Index**: `zIndex: 1200` to stay above content
- Toggle Type: Must be toggleable and main screen area must adjust according to toggle.
- Common Component: Must be applied as common component to always exist regardless of page routing connection

### Sidebar Component
```typescript
<Drawer variant="permanent" sx=\{{ width: 240, '& .MuiDrawer-paper': { width: 240 } }}>
  <Box 
    sx=\{{ 
      p: 2, 
      cursor: 'pointer',
      '&:hover': {
        backgroundColor: 'rgba(0, 0, 0, 0.04)',
      },
      borderRadius: 1,
      mx: 1,
    }}
    onClick={() => navigate('/')}
  >
    <Typography variant="h6" color="primary">odooerp</Typography>
  </Box>
  <List>
    <ListItem button onClick={() => navigate('/dashboard')}>
      <ListItemIcon><Dashboard /></ListItemIcon>
      <ListItemText primary="Dashboard" />
    </ListItem>
  </List>
</Drawer>
```

### Responsive Sidebar
```typescript
// Mobile
<Drawer variant="temporary" open={mobileOpen} sx=\{{ display: { xs: 'block', md: 'none' } }}>
  {/* Sidebar content */}
</Drawer>

// Desktop  
<Drawer variant="permanent" sx=\{{ display: { xs: 'none', md: 'block' } }}>
  {/* Sidebar content */}
</Drawer>
```

### Sidebar Navigation Items
- **Active State**: `selected` prop with `bgcolor: 'action.selected'`
- **Icons**: Material-UI icons with consistent size
- **Nested Items**: Use `Collapse` for sub-navigation


## Important Notes
1. When generating basic files, must create React-based project compatible with Node22 at a level that doesn't cause execution errors.
2. All work must start with creating frontend folder, and subsequent work must proceed in frontend subdirectory.
3. When configuring main page, must strictly adhere to full window size.
4. Generate components and features based only on what is explicitly required in this PRD - do not add dashboard statistics, mock data, or decorative elements unless explicitly requested.
